spring:
  application:
    name: uaa
  config:
    activate:
      on-profile: docker

  datasource:
    url: jdbc:mysql://mysql:3306/uaa_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: root_password
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration
    baseline-version: 0

  security:
    oauth2:
      authorization-server:
        enabled: true
      client:
        registration:
          github:
            client-id: ${GITHUB_CLIENT_ID:Ov23lit971NDwRkcC7UJ}
            client-secret: ${GITHUB_CLIENT_SECRET:0f5e7039506c6b5854f674931b70a8de673ed80d}
            scope: read:user,user:email
        provider:
          github:
            authorization-uri: https://github.com/login/oauth/authorize
            token-uri: https://github.com/login/oauth/access_token
            user-info-uri: https://api.github.com/user
            user-name-attribute: login

  ldap:
    urls: ldap://openldap:389
    base: dc=luban-cae,dc=com
    manager-dn: cn=admin,dc=luban-cae,dc=com
    manager-password: admin_secret

server:
  port: 9000
  error:
    include-message: always
    include-binding-errors: always

app:
  issuer: http://uaa:9000
  # Override with external URL for client redirects

logging:
  level:
    root: INFO
    org.springframework.security: DEBUG
    org.springframework.ldap: DEBUG
    org.springframework.security.ldap: DEBUG
